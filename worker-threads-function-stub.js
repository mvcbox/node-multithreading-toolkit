'use strict';

/**
 *
 */
module.exports = function () {
    throw new Error('Not supported for current Node.js version [' + process.version + ']. This functionality is supported for version v10.5.0 and higher.');
};
